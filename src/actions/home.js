import { HOME_INFO } from '../constants/home'
import {  } from '../constants/api'
import { createAction } from '../service/servers'

/**
 * 首页数据
 * @param {*} postData 
 */
export const dispatchHomeIndex = postData => createAction({
    
})